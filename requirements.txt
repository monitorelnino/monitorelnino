# Versões travadas em 27/08/2026 para reprodutibilidade auditável (docs/SBOM.md
# documenta o porquê e como reabrir a faixa se uma atualização for necessária).
requests==2.33.1
numpy==2.4.4
openpyxl==3.1.5
reportlab==4.4.10
pypdf==5.9.0
# 11/09/2026: pdfplumber era IMPORTADO pelos quatro coletores de boletim estadual (MS, DF, PE, PB)
# sem nunca constar aqui — no runner eles morriam com ModuleNotFoundError, e nenhum jamais entregou série.
# Trocar por pypdf não resolve: nos PDFs-infográfico das SES o pypdf extrai com espaço entre caracteres
# ("1 1 . 2 4 1", "S E  0 1  a  3 5"), e nenhum padrão numérico casa. pdfplumber preserva o texto como os
# parsers foram afinados. Versão travada, como todo o resto (docs/SBOM.md).
pdfplumber==0.11.9
# 23/09/2026 (§177): pypdfium2 e Pillow já vinham como dependências TRANSITIVAS do pdfplumber, mas
# o OCR dos PLANCON escaneados as usa diretamente (rasterizar a página e salvar o PNG que vai ao
# Tesseract). Dependência usada de propósito se declara e se trava: a versão do rasterizador decide
# o pixel, e o pixel decide o texto que fica preservado — faixa aberta significaria cópia preservada
# variando com a máquina. Nenhum pacote novo entra: são as mesmas duas que o pdfplumber já instalava.
pypdfium2==5.13.0
Pillow==12.3.0
PyYAML==6.0.3
Markdown==3.10.2
# 24/09/2026: certifi já vinha como dependência TRANSITIVA do requests, mas coletar_sinais_risco.py
# passou a usá-lo DIRETAMENTE para verificar o certificado do gsc.cemaden.gov.br, cuja cadeia a loja
# do Windows não completa (CERTIFICATE_VERIFY_FAILED, que parecia fonte fora do ar e era ambiente).
# Mesma regra do pypdfium2 acima: dependência usada de propósito se declara e se trava — aqui a versão
# do pacote de raízes decide QUAIS FONTES a coleta consegue verificar, e faixa aberta significaria
# cobertura variando com a máquina. Não afrouxa verificação nenhuma: só amplia o conjunto de raízes.
certifi==2026.7.22
